import React from 'react';
import NavBar from '../components/headers/NavBar';

const MainLayout = () => {
    return (
        <main>
            <NavBar />
        </main>
    );
};

export default MainLayout;